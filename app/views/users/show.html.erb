<% provide(:title, @user.name) %>
<div class="row">
  <aside class="col-md-4">
    <section class="user_info">
      <h1>
        <%= gravatar_for @user %>
        <%= @user.name %>
      </h1>      
    </section>
  </aside>
</div>

<%= @user.name %>, <%= @user.email %> was created at <%= @user.created_at %>.

When you hear the beep, it will be <%= Time.now.strftime('%c') %>